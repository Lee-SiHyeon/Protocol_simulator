/******************************************************************************/
// @copyright   Copyright Notification
//   No part may be reproduced except as authorized by written permission.
//   The copyright and the foregoing restriction extend to reproduction in all media.
//   (c) 2026, 3GPP Organizational Partners (ARIB, ATIS, CCSA, ETSI, TSDSI, TTA, TTC).
//   All rights reserved.
// @version: 36.523-3 v18.11.0
// $Date: 2020-06-06 15:43:35 +0200 (Sat, 06 Jun 2020) $
// $Rev: 27846 $
/******************************************************************************/
module EUTRA_RSRQ_Functions {

  import from Common4G5G_Templates all;
  import from CommonDefs all;
  import from EUTRA_ASP_VirtualNoiseDefs all;
  import from EUTRA_AspCommon_Templates all;
  import from EUTRA_CellInfo all;
  import from EUTRA_Component all;
  import from EUTRA_NB_CommonDefs all;

  template (value) EUTRA_VNG_CTRL_REQ cas_EUTRA_VNG_Configure_REQ(EUTRA_CellId_Type p_CellId,
                                                                  Dl_Bandwidth_Type p_DL_Bandwidth,
                                                                  integer  p_NocLevel ):=
  { /* @status    APPROVED (LTE, LTE_A_IRAT, LTE_A_PRO, LTE_IRAT, NR5GC_IRAT) */
    Common := cs_ReqAspCommonPart_CellCfg(p_CellId, cs_TimingInfo_Now),
    Request := {
     Configure := {
       Bandwidth := p_DL_Bandwidth,
       NocLevel := p_NocLevel
     }
    }
  };

  template (value) EUTRA_VNG_CTRL_REQ cas_EUTRA_VNG_Activate_REQ (EUTRA_CellId_Type p_CellId) :=
  { /* @status    APPROVED (LTE, LTE_A_IRAT, LTE_A_PRO, LTE_IRAT, NR5GC_IRAT) */
    Common := cs_ReqAspCommonPart_CellCfg(p_CellId, cs_TimingInfo_Now),
    Request := {
      Activate := true
    }
  };

  /*
   * @desc      configuration of the virtual noise generator, regardless of the power level the noise generator is off before it gets activated for this cell
   * @param     p_CellId
   * @param     p_NocLevel
   * @status    APPROVED (LTE, LTE_A_IRAT, LTE_A_PRO, LTE_IRAT, NR5GC_IRAT)
   */
  function f_EUTRA_ConfigureVNG(EUTRA_CellId_Type p_CellId,
                                integer  p_NocLevel) runs on EUTRA_PTC
  {
    VNG.send (cas_EUTRA_VNG_Configure_REQ ( p_CellId, f_EUTRA_CellInfo_GetDL_ChBandwidth(p_CellId), p_NocLevel)) ;
  }

  /*
   * @desc      Activate the noise (switched on) for the given cell
   * @param     p_CellId
   * @status    APPROVED (LTE, LTE_A_IRAT, LTE_A_PRO, LTE_IRAT, NR5GC_IRAT)
   */
  function f_EUTRA_ActivateVNG ( EUTRA_CellId_Type p_CellId) runs on EUTRA_PTC
  {
    VNG.send (cas_EUTRA_VNG_Activate_REQ ( p_CellId)) ;
  }

}

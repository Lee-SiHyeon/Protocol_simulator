/******************************************************************************/
// @copyright   Copyright Notification
//   No part may be reproduced except as authorized by written permission.
//   The copyright and the foregoing restriction extend to reproduction in all media.
//   (c) 2026, 3GPP Organizational Partners (ARIB, ATIS, CCSA, ETSI, TSDSI, TTA, TTC).
//   All rights reserved.
// @version: 38.523-3 v18.6.0
// $Date: 2026-02-24 13:18:29 +0100 (Tue, 24 Feb 2026) $
// $Rev: 42904 $
/******************************************************************************/

module EUTRA_AuxiliaryCapCheckFunctions {

  import from EUTRA_RRC_ASN1_Definitions language "ASN.1:2002" all with {encode "UNALIGNED_PER_OctetAligned"};
  import from CommonDefs all;
  import from EUTRA_Parameters all;

  type UE_EUTRA_Capability_v1730_IEs.nonCriticalExtension.nonCriticalExtension.nonCriticalExtension.nonCriticalExtension.measParameters_v1840.simultaneousRxDataSSB_DiffNumerology_FR1_r18 SimultaneousRxDataSSB_DiffNumerology_FR1_r18_Type; /* @status    APPROVED (ENDC, LTE) */

  /*
   * @desc      simultaneousRxDataSSB_DiffNumerology_FR1_r18 must be supported if pc_simultaneousRxDataSSB_DiffNumerology_FR1_r18
   * @return    template SimultaneousRxDataSSB_DiffNumerology_FR1_r18_Type
   * @status    APPROVED (ENDC, LTE)
   */
  function f_Derive_SimultaneousRxDataSSB_DiffNumerology_FR1_r18() return template SimultaneousRxDataSSB_DiffNumerology_FR1_r18_Type
  {// @sic R5-255037 sic@
    if (pc_simultaneousRxDataSSB_DiffNumerology_FR1_r18) {
      return supported;
    } else {
      return omit;
    }
  }

  /*
   * @desc      To decode the EUTRA UL DCCH message containing EUTRA capabilities
   * @param     p_Octetstring
   * @return    UL_DCCH_Message
   * @status    APPROVED (ENDC, LTE, LTE_A_PRO, LTE_A_R10_R11)
   */
  function f_EUTRA_DecodeULDcchMsg(octetstring p_Octetstring) return UL_DCCH_Message
  {
    var UL_DCCH_Message v_UL_Dcch_Msg;
    var bitstring v_Bitstring := oct2bit(p_Octetstring);
    var integer v_Result := decvalue(v_Bitstring, v_UL_Dcch_Msg);
    if (v_Result != 0) {
      FatalError(__FILE__, __LINE__, "UE EUTRA Capability cannot be decoded");
    }
    return v_UL_Dcch_Msg;  // @sic R5s230454 sic@
  }

}

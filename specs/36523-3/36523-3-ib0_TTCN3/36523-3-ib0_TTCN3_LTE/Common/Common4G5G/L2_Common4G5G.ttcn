/******************************************************************************/
// @copyright   Copyright Notification
//   No part may be reproduced except as authorized by written permission.
//   The copyright and the foregoing restriction extend to reproduction in all media.
//   (c) 2026, 3GPP Organizational Partners (ARIB, ATIS, CCSA, ETSI, TSDSI, TTA, TTC).
//   All rights reserved.
// @version: 36.523-3 v18.11.0
// $Date: 2020-06-24 13:34:32 +0200 (Wed, 24 Jun 2020) $
// $Rev: 28251 $
/******************************************************************************/

module L2_Common4G5G {

  import from CommonDefs all;

  const B3_Type tsc_B3_Reserved := '000'B;   /* @status    APPROVED (ENDC, LTE, LTE_A_PRO, LTE_A_R10_R11, LTE_A_R12, NR5GC) */

  const integer tsc_RLC_SDU1 := 0;                        /* @status    APPROVED (ENDC, LTE, NBIOT, NR5GC) */

  const integer tsc_RLC_SDU2 := 1;                        /* @status    APPROVED (ENDC, LTE, NBIOT, NR5GC) */

  const integer tsc_RLC_SDU3 := 2;                        /* @status    APPROVED (ENDC, LTE, NBIOT, NR5GC) */

  const integer tsc_RLC_SDU4 := 3;                        /* @status    APPROVED (ENDC, LTE, NBIOT, NR5GC) */

  const integer tsc_RLC_SDU5 := 4;                        /* @status    APPROVED (ENDC, LTE, NBIOT, NR5GC) */

  const integer tsc_RLC_SDU6 := 5;                        /* @status    APPROVED (ENDC, LTE, NBIOT, NR5GC) */

  const integer tsc_RLC_SDU7 := 6;                        /* @status    APPROVED (ENDC, LTE, NBIOT, NR5GC) */

  const integer tsc_RLC_SDU8 := 7;                        /* @status    APPROVED (ENDC, LTE, NBIOT, NR5GC) */

  const integer tsc_RLC_SDU9 := 8;                        /* @status    APPROVED (ENDC, LTE, NBIOT, NR5GC) */

  const integer tsc_RLC_SDU10 := 9;                       /* @status    APPROVED (ENDC, LTE, NBIOT, NR5GC) */

  const integer tsc_RLC_SDU11 := 10;                      /* @status    APPROVED (ENDC, LTE, NBIOT, NR5GC) */

  const integer tsc_RLC_SDU12 := 11;                      /* @status    APPROVED (ENDC, LTE, NBIOT, NR5GC) */

  const integer tsc_RLC_SDU13 := 12;                      /* @status    APPROVED (ENDC, LTE, NBIOT, NR5GC) */

  const integer tsc_RLC_SDU14 := 13;                      /* @status    APPROVED (ENDC, LTE, NBIOT, NR5GC) */

  const integer tsc_RLC_SDU15 := 14;                      /* @status    APPROVED (ENDC, LTE, NBIOT, NR5GC) */

  const integer tsc_RLC_SDU16 := 15;                      /* @status    APPROVED (ENDC, LTE, NBIOT, NR5GC) */

  const integer tsc_RLC_SDU17 := 16;                      /* @status    APPROVED (ENDC, LTE, NBIOT, NR5GC) */

  const integer tsc_RLC_SDU18 := 17;                      /* @status    APPROVED (ENDC, LTE, NBIOT, NR5GC) */

  const integer tsc_RLC_SDU19 := 18;                      /* @status    APPROVED (ENDC, LTE, NBIOT, NR5GC) */

  const integer tsc_RLC_SDU20 := 19;                      /* @status    APPROVED (ENDC, LTE, NBIOT, NR5GC) */

  const integer tsc_RLC_SDU21 := 20;                      /* @status    APPROVED (LTE, NBIOT) */

  const integer tsc_RLC_SDU22 := 21;                      /* @status    APPROVED (LTE, NBIOT) */

  const integer tsc_MaxPRBS_Pos := 16777215;              /* @desc     Maximum octet position for accessing PRBS.
                                                           * @status    APPROVED (ENDC, LTE, NBIOT, NR5GC) */

}

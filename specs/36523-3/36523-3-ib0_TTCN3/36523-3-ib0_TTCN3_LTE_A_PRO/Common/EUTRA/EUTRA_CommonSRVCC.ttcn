/******************************************************************************/
// @copyright   Copyright Notification
//   No part may be reproduced except as authorized by written permission.
//   The copyright and the foregoing restriction extend to reproduction in all media.
//   (c) 2026, 3GPP Organizational Partners (ARIB, ATIS, CCSA, ETSI, TSDSI, TTA, TTC).
//   All rights reserved.
// @version: 36.523-3 v18.11.0
// $Date: 2022-10-06 08:02:51 +0200 (Thu, 06 Oct 2022) $
// $Rev: 34385 $
/******************************************************************************/

module EUTRA_CommonSRVCC {

  import from EUTRA_RRC_ASN1_Definitions language "ASN.1:2002" all with {encode "UNALIGNED_PER_OctetAligned"};
  import from CommonDefs all;
  import from EUTRA_Component all;
  import from EUTRA_NASSteps all;
  import from IMS_PTC_CoordMsg all;

  /*
   * @desc      Common function to establish MO IMS speech call over LTE
   * @param     p_CellId
   * @status    APPROVED (LTE_A_IRAT, LTE_A_PRO)
   */
  function f_EUTRA_GenericIMS_MO_SpeechCallInitiation(EUTRA_CellId_Type p_CellId)  runs on EUTRA_PTC
  {
    // Trigger the UE to make the call
    f_IMS_IPCAN_SendCoOrdMsg(IMS[tsc_Index_IMS1]);
    
    //@siclog "Steps 1-8" siclog@
    f_EUTRA_ServiceRequestAndActivate_SRB2_DRB(p_CellId); // @sic R5-154047 sic@
    // Send trigger to IMS PTC
    // Steps 9 to 11 are executed in IMS PTC
    f_IMS_IPCAN_SendCoOrdMsg(IMS[tsc_Index_IMS1]);
    f_IMS_IPCAN_WaitForTrigger(IMS[tsc_Index_IMS1]); // @sic R5s140572 sic@
  }

}

/******************************************************************************/
// @copyright   Copyright Notification
//   No part may be reproduced except as authorized by written permission.
//   The copyright and the foregoing restriction extend to reproduction in all media.
//   (c) 2026, 3GPP Organizational Partners (ARIB, ATIS, CCSA, ETSI, TSDSI, TTA, TTC).
//   All rights reserved.
// @version: 38.523-3 v18.6.0
// $Date: 2024-06-24 14:28:38 +0200 (Mon, 24 Jun 2024) $
// $Rev: 39009 $
/******************************************************************************/

module MAC_DRX_ENDC_NR {

  import from NR_RRC_ASN1_Definitions language "ASN.1:2002" all with {encode "UNALIGNED_PER_OctetAligned"};
  import from Common4G5G_Templates all;
  import from DRX_MAC_TC_Common_NR all;
  import from EUTRA_NR_CoordMsg all;
  import from NR_ASP_TypeDefs all;
  import from NR_CellCfg_Templates all;
  import from NR_CellInfo all;
  import from NR_CellInfoInit_ENDC all;
  import from NR_CommonDefs all;
  import from NR_CommonFunctions all;
  import from NR_CommonFunctions_ENDC all;
  import from NR_Component_Base all;
  import from NR_Component_ENDC all;
  import from NR_ConfigurationSteps all;
  import from NR_L2_Common all;
  import from NR_RRC_Templates all;

  /*
   * @desc      REFERENCE TS 38.523-1 clause 7.1.1.5.1.ENDC
   * @status    APPROVED (ENDC)
   */
  function f_TC_7_1_1_5_1_ENDC_NR() runs on ENDC_NR_PTC
  {// DRX operation / Short cycle not configured / Parameters configured by RRC
    

    var DRB_Identity v_DRBId :=tsc_NR_DRB2;
    var template (value) MAC_CellGroupConfig v_MAC_CellGroupConfig;
    var template (value)  NR_RadioBearerList_Type v_SS_Drb_ConfigList  := {cs_NR_SS_RadioBearer_MAC_TestMode(tsc_NR_DRB2, cs_NR_MAC_TestModeInfo_NormalMode)};
    var template (value) CellGroupConfig v_CellGroupConfig;

    //Init Cell parameters
    f_ENDC_NR_Init();
    

    //Create and configure NR cell1
    f_NR_CellConfig_Def(nr_Cell1);
    
    // Get NR capabilities sent during EUTRA preamble
    f_NR_ENDC_PreambleOnEUTRA(nr_Cell1, MCG_Only);
    f_NR_AS_CipheringAlgorithm_Set(nea0);

    //Configure on SS the SCG DRB in No MAC header manipulation in DL and RLC and PDCP in transparent mode
    f_NR_ENDC_ConfigAM_UM(nr_Cell1, 1, 0, v_SS_Drb_ConfigList);
    // Wait for Trigger from EUTRA indicating start of Test Body
    f_EUTRA_NR_WaitForCoOrd_Trigger(EUTRA);
     
    f_NR_TestBody_Set(true);

    //@siclog steps 1-2 siclog@
    v_MAC_CellGroupConfig := f_NR_CellInfo_GetMAC_CellGroupConfig(nr_Cell1);
    v_MAC_CellGroupConfig.drx_Config := cs_NR_DRX_Config_Setup ( cs_NR_DRX_Config_Def( cs_NR_DRX_OnDurationTimerMs20, ms10, 56, 56, sl8, sl8, cs_NR_DRX_LongCycleStartOffsetMs640_7, -, 0));
    f_NR_CellInfo_SetMAC_CellGroupConfig(nr_Cell1, v_MAC_CellGroupConfig);
    v_CellGroupConfig := cs_38508_CellGroupConfig (tsc_NR_CellGroupId_SCG,
                                                   -,
                                                   -,
                                                   v_MAC_CellGroupConfig);
    f_NR_SendRRCReconfigurationContentsToEUTRA (omit, v_CellGroupConfig);
    //@sic R5s201535 sic@
    // stop periodic UL grants and restart automatic grant allocation instead; periodic TA is kept stopped:
    f_NR_ULGrantConfiguration_Start(nr_Cell1, cs_TimingInfo_Now, -,-, cs_NR_UplinkTimeAlignment_Stop);

    v_SS_Drb_ConfigList  := {cs_NR_SS_RadioBearer_MAC_TestMode(tsc_NR_DRB2, cs_NR_MAC_TestModeInfo_NoHeaderManipulationDL_UL)};
    f_NR_SS_CommonRadioBearerConfig(nr_Cell1, v_SS_Drb_ConfigList);

     //@siclog steps 3-20 siclog@
     f_TC_7_1_1_5_1_NR_TestBody(v_DRBId);


    f_NR_TestBody_Set(false);
    // Send Trigger to EUTRA indicating Finish of Test Body
    f_EUTRA_NR_SendCoOrd(EUTRA, cms_EUTRA_NR_Trigger);

    //Wait from coordination AFTER the connection is released in E-UTRA
    f_EUTRA_NR_WaitForCoOrd_Trigger(EUTRA, "Postamble");
    //Postamble
    f_NR_ReleaseAllCells();
  }

  /*
   * @desc      REFERENCE TS 38.523-1 clause 7.1.1.5.2.ENDC
   * @status    APPROVED (ENDC)
   */
  function f_TC_7_1_1_5_2_ENDC_NR() runs on ENDC_NR_PTC
  {// DRX operation / Short cycle not configured / Parameters configured by RRC
    

    var DRB_Identity v_DRBId :=tsc_NR_DRB2;
    var template (value) MAC_CellGroupConfig v_MAC_CellGroupConfig;
    var template (value)  NR_RadioBearerList_Type v_SS_Drb_ConfigList  := {cs_NR_SS_RadioBearer_MAC_TestMode(tsc_NR_DRB2, cs_NR_MAC_TestModeInfo_NormalMode)};
    var template (value) CellGroupConfig v_CellGroupConfig;
    
    //Init Cell parameters
    f_ENDC_NR_Init();
    

    //Create and configure NR cell1
    f_NR_CellConfig_Def(nr_Cell1);
    
    // Get NR capabilities sent during EUTRA preamble
    f_NR_ENDC_PreambleOnEUTRA(nr_Cell1, MCG_Only);
    f_NR_AS_CipheringAlgorithm_Set(nea0);

    //Configure on SS the SCG DRB in No MAC header manipulation in DL and RLC and PDCP in transparent mode
    f_NR_ENDC_ConfigAM_UM(nr_Cell1, 1, 0, v_SS_Drb_ConfigList);
    // Wait for Trigger from EUTRA indicating start of Test Body
    f_EUTRA_NR_WaitForCoOrd_Trigger(EUTRA);
     
    f_NR_TestBody_Set(true);

    //@siclog steps 1-2 siclog@
    v_MAC_CellGroupConfig := f_NR_CellInfo_GetMAC_CellGroupConfig(nr_Cell1);
    v_MAC_CellGroupConfig.drx_Config := cs_NR_DRX_Config_Setup ( cs_NR_DRX_Config_Def( cs_NR_DRX_OnDurationTimerMs40, ms10, 56, 56, sl80, sl80, cs_NR_DRX_LongCycleStartOffsetMs640_7, -, 0));
    f_NR_CellInfo_SetMAC_CellGroupConfig(nr_Cell1, v_MAC_CellGroupConfig);
    v_CellGroupConfig := cs_38508_CellGroupConfig (tsc_NR_CellGroupId_SCG,
                                                   -,
                                                   -,
                                                   v_MAC_CellGroupConfig);
    f_NR_SendRRCReconfigurationContentsToEUTRA (omit, v_CellGroupConfig);

    v_SS_Drb_ConfigList  := {cs_NR_SS_RadioBearer_MAC_TestMode(tsc_NR_DRB2, cs_NR_MAC_TestModeInfo_NoHeaderManipulationDL_UL)};
    f_NR_SS_CommonRadioBearerConfig(nr_Cell1, v_SS_Drb_ConfigList);


     //@siclog steps 3-12 siclog@
     f_TC_7_1_1_5_2_NR_TestBody(v_DRBId);


    f_NR_TestBody_Set(false);
    // Send Trigger to EUTRA indicating Finish of Test Body
    f_EUTRA_NR_SendCoOrd(EUTRA, cms_EUTRA_NR_Trigger);

    //Wait from coordination AFTER the connection is released in E-UTRA
    f_EUTRA_NR_WaitForCoOrd_Trigger(EUTRA, "Postamble");
    //Postamble
    f_NR_ReleaseAllCells();
  }

  /*
   * @desc      REFERENCE TS 38.523-1 clause 7.1.1.5.3.ENDC
   * @status    APPROVED (ENDC)
   */
  function f_TC_7_1_1_5_3_ENDC_NR() runs on ENDC_NR_PTC
  {// DRX operation / Short cycle not configured / Parameters configured by RRC

    var DRB_Identity v_DRBId :=tsc_NR_DRB2;
    var template (value) MAC_CellGroupConfig v_MAC_CellGroupConfig;
    var template (value)  NR_RadioBearerList_Type v_SS_Drb_ConfigList  := {cs_NR_SS_RadioBearer_MAC_TestMode(tsc_NR_DRB2, cs_NR_MAC_TestModeInfo_NormalMode)};
    var template (value) CellGroupConfig v_CellGroupConfig;

    //Init Cell parameters
    f_ENDC_NR_Init();

    //Create and configure NR cell1
    f_NR_CellConfig_Def(nr_Cell1);
    
    // Get NR capabilities sent during EUTRA preamble
    f_NR_ENDC_PreambleOnEUTRA(nr_Cell1, MCG_Only);
    f_NR_AS_CipheringAlgorithm_Set(nea0);

    //Configure on SS the SCG DRB in No MAC header manipulation in DL and RLC and PDCP in transparent mode
    f_NR_ENDC_ConfigAM_UM(nr_Cell1, 1, 0, v_SS_Drb_ConfigList);
    // Wait for Trigger from EUTRA indicating start of Test Body
    f_EUTRA_NR_WaitForCoOrd_Trigger(EUTRA);
     
    f_NR_TestBody_Set(true);

    //@siclog steps 1-2 siclog@
    v_MAC_CellGroupConfig := f_NR_CellInfo_GetMAC_CellGroupConfig(nr_Cell1);
    v_MAC_CellGroupConfig.drx_Config := cs_NR_DRX_Config_Setup ( cs_NR_DRX_Config_Def( cs_NR_DRX_OnDurationTimerMs20, ms10, 56, 56, sl80, sl80, cs_NR_DRX_LongCycleStartOffsetMs640_7, cs_ShortDRX_Config_715X, 0));
    f_NR_CellInfo_SetMAC_CellGroupConfig(nr_Cell1, v_MAC_CellGroupConfig);
    v_CellGroupConfig := cs_38508_CellGroupConfig (tsc_NR_CellGroupId_SCG,
                                                   -,
                                                   -,
                                                   v_MAC_CellGroupConfig);
    f_NR_SendRRCReconfigurationContentsToEUTRA (omit, v_CellGroupConfig);
    //@sic R5s201053 sic@
    // stop periodic UL grants and restart automatic grant allocation instead; periodic TA is kept stopped:
    f_NR_ULGrantConfiguration_Start(nr_Cell1,-, -, -, cs_NR_UplinkTimeAlignment_Stop);


    v_SS_Drb_ConfigList  := {cs_NR_SS_RadioBearer_MAC_TestMode(tsc_NR_DRB2, cs_NR_MAC_TestModeInfo_NoHeaderManipulationDL_UL)};
    f_NR_SS_CommonRadioBearerConfig(nr_Cell1, v_SS_Drb_ConfigList);


    //@siclog steps 3-20 siclog@
    f_TC_7_1_1_5_3_NR_TestBody(v_DRBId);

    f_NR_TestBody_Set(false);
    // Send Trigger to EUTRA indicating Finish of Test Body
    f_EUTRA_NR_SendCoOrd(EUTRA, cms_EUTRA_NR_Trigger);

    //Wait from coordination AFTER the connection is released in E-UTRA
    f_EUTRA_NR_WaitForCoOrd_Trigger(EUTRA, "Postamble");
    //Postamble
    f_NR_ReleaseAllCells();
  }

  /*
   * @desc      REFERENCE TS 38.523-1 clause 7.1.1.5.4.ENDC
   * @status    APPROVED (ENDC)
   */
  function f_TC_7_1_1_5_4_ENDC_NR() runs on ENDC_NR_PTC
  {// DRX operation / Short cycle configured / Long DRX command MAC control element reception
  

    var DRB_Identity v_DRBId :=tsc_NR_DRB2;
    var template (value) MAC_CellGroupConfig v_MAC_CellGroupConfig;
    var template (value)  NR_RadioBearerList_Type v_SS_Drb_ConfigList  := {cs_NR_SS_RadioBearer_MAC_TestMode(tsc_NR_DRB2, cs_NR_MAC_TestModeInfo_NormalMode)};
    var template (value) CellGroupConfig v_CellGroupConfig;
    
    //Init Cell parameters
    f_ENDC_NR_Init();
    

    //Create and configure NR cell1
    f_NR_CellConfig_Def(nr_Cell1);
    
    // Get NR capabilities sent during EUTRA preamble
    f_NR_ENDC_PreambleOnEUTRA(nr_Cell1, MCG_Only);
    f_NR_AS_CipheringAlgorithm_Set(nea0);

    //Configure on SS the SCG DRB in No MAC header manipulation in DL and RLC and PDCP in transparent mode
    f_NR_ENDC_ConfigAM_UM(nr_Cell1, 1, 0, v_SS_Drb_ConfigList);
    // Wait for Trigger from EUTRA indicating start of Test Body
    f_EUTRA_NR_WaitForCoOrd_Trigger(EUTRA);
     
    f_NR_TestBody_Set(true);

    //@siclog steps 1-2 siclog@
    v_MAC_CellGroupConfig := f_NR_CellInfo_GetMAC_CellGroupConfig(nr_Cell1);
    //@sic R5s200928 sic@
    v_MAC_CellGroupConfig.drx_Config := cs_NR_DRX_Config_Setup ( cs_NR_DRX_Config_Def( cs_NR_DRX_OnDurationTimerMs40, ms10, 56, 56, sl80, sl80, cs_NR_DRX_LongCycleStartOffsetMs640_7, cs_ShortDRX_Config_715X, 0)); //@sic r5s201517 sic@
    f_NR_CellInfo_SetMAC_CellGroupConfig(nr_Cell1, v_MAC_CellGroupConfig);
    v_CellGroupConfig := cs_38508_CellGroupConfig (tsc_NR_CellGroupId_SCG,
                                                   -,
                                                   -,
                                                   v_MAC_CellGroupConfig);
    f_NR_SendRRCReconfigurationContentsToEUTRA (omit, v_CellGroupConfig);


    v_SS_Drb_ConfigList  := {cs_NR_SS_RadioBearer_MAC_TestMode(tsc_NR_DRB2, cs_NR_MAC_TestModeInfo_NoHeaderManipulationDL_UL)};
    f_NR_SS_CommonRadioBearerConfig(nr_Cell1, v_SS_Drb_ConfigList);
   
     //@siclog steps 3-12 siclog@
     f_TC_7_1_1_5_4_NR_TestBody(v_DRBId);


    f_NR_TestBody_Set(false);
    // Send Trigger to EUTRA indicating Finish of Test Body
    f_EUTRA_NR_SendCoOrd(EUTRA, cms_EUTRA_NR_Trigger);

    //Wait from coordination AFTER the connection is released in E-UTRA
    f_EUTRA_NR_WaitForCoOrd_Trigger(EUTRA, "Postamble");
    //Postamble
    f_NR_ReleaseAllCells();
  };

  /*
   * @desc      REFERENCE TS 38.523-1 clause 7.1.1.5.5.ENDC
   * @status    APPROVED (ENDC)
   */
  function f_TC_7_1_1_5_5_ENDC_NR() runs on ENDC_NR_PTC
  {// DRX Operation / Short cycle configured / DRX command MAC control element reception
    var DRB_Identity v_DRBId :=tsc_NR_DRB2;
    var template (value) MAC_CellGroupConfig v_MAC_CellGroupConfig;
    var template (value)  NR_RadioBearerList_Type v_SS_Drb_ConfigList  := {cs_NR_SS_RadioBearer_MAC_TestMode(tsc_NR_DRB2, cs_NR_MAC_TestModeInfo_NormalMode)};
    var template (value) CellGroupConfig v_CellGroupConfig;
    
    //Init Cell parameters
    f_ENDC_NR_Init();

    //Create and configure NR cell1
    f_NR_CellConfig_Def(nr_Cell1);
    
    // Get NR capabilities sent during EUTRA preamble
    f_NR_ENDC_PreambleOnEUTRA(nr_Cell1, MCG_Only);
    f_NR_AS_CipheringAlgorithm_Set(nea0);

    //Configure on SS the SCG DRB in No MAC header manipulation in DL and RLC and PDCP in transparent mode
    f_NR_ENDC_ConfigAM_UM(nr_Cell1, 1, 0, v_SS_Drb_ConfigList);
    // Wait for Trigger from EUTRA indicating start of Test Body
    f_EUTRA_NR_WaitForCoOrd_Trigger(EUTRA);
     
    f_NR_TestBody_Set(true);

    //@siclog steps 1-2 siclog@
    v_MAC_CellGroupConfig := f_NR_CellInfo_GetMAC_CellGroupConfig(nr_Cell1);
    v_MAC_CellGroupConfig.drx_Config := cs_NR_DRX_Config_Setup ( cs_NR_DRX_Config_Def( cs_NR_DRX_OnDurationTimerMs40, ms10, 56, 56, sl80, sl80, cs_NR_DRX_LongCycleStartOffsetMs640_7, cs_ShortDRX_Config_715X, 0)); //@sic R5s201495 sic@
    f_NR_CellInfo_SetMAC_CellGroupConfig(nr_Cell1, v_MAC_CellGroupConfig);
    v_CellGroupConfig := cs_38508_CellGroupConfig (tsc_NR_CellGroupId_SCG,
                                                   -,
                                                   -,
                                                   v_MAC_CellGroupConfig);
    f_NR_SendRRCReconfigurationContentsToEUTRA (omit, v_CellGroupConfig);


    v_SS_Drb_ConfigList  := {cs_NR_SS_RadioBearer_MAC_TestMode(tsc_NR_DRB2, cs_NR_MAC_TestModeInfo_NoHeaderManipulationDL_UL)};
    f_NR_SS_CommonRadioBearerConfig(nr_Cell1, v_SS_Drb_ConfigList);
   
     //@siclog steps 3-12 siclog@
     f_TC_7_1_1_5_5_NR_TestBody(v_DRBId);

    f_NR_TestBody_Set(false);
    // Send Trigger to EUTRA indicating Finish of Test Body
    f_EUTRA_NR_SendCoOrd(EUTRA, cms_EUTRA_NR_Trigger);

    //Wait from coordination AFTER the connection is released in E-UTRA
    f_EUTRA_NR_WaitForCoOrd_Trigger(EUTRA, "Postamble");
    //Postamble
    f_NR_ReleaseAllCells();
  };

}
